services:
  ollama:
    image: ollama/ollama:latest
    container_name: free_fiesta_ollama
    entrypoint: ["/bin/sh", "/app/startup/ollama-init.sh"]
    volumes:
      - ./startup:/app/startup
      - ollama_data:/root/.ollama
    ports:
      - "11434:11434"
    restart: always

  worker:
    container_name: free_fiesta_worker
    build: ./worker
    shm_size: '10.24gb'
    ports:
      - "9000:9000"  # Ray Serve HTTP Port
      - "8265:8265"  # Ray Dashboard
    environment:
      - OLLAMA_URL=http://ollama:11434
      - VLLM_URL=http://34.0.43.79:8001
      - PYTHONUNBUFFERED=1
      # ✅ This tells Ray Serve where to bind (Replaces the --host flag)
      - RAY_SERVE_HTTP_HOST=0.0.0.0
      - RAY_SERVE_HTTP_PORT=9000
    depends_on:
      - ollama
    volumes:
      - ./worker:/app           
      - ~/.cache/huggingface:/root/.cache/huggingface
    
    # ✅ FINAL CORRECTED COMMAND:
    # 1. 'ray start' sets up the cluster.
    # 2. 'serve run' only needs the file path. (No --host/--port flags allowed here!)
    command: >
      /bin/bash -c "
      ray start --head --dashboard-host=0.0.0.0 --port=6379 --dashboard-port=8265 --disable-usage-stats --block & 
      sleep 10 && 
      serve run ray_app:ingress"
      
  api:
    container_name: free_fiesta_api
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - worker
    volumes:
      - ./backend:/app
      - ./tests:/app/tests
      - ./allure-results:/app/allure-results
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --timeout-keep-alive 0

  ui:
    container_name: free_fiesta_ui
    build: ./ui
    ports:
      - "3000:3000"
    depends_on:
      - api

volumes:
  ollama_data: