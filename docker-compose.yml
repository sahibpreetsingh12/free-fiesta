version: "3.9"

services:

  api:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - worker

  worker:
    build: ./worker
    ports:
      - "9000:9000"

  ui:
    build: ./ui
    ports:
      - "3000:80"
    depends_on:
      - api

